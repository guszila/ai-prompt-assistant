# Cross-Cutting & Integration Tests

This directory contains end-to-end and cross-cutting integration tests.

- Service unit and API tests reside in `apps/ai-service/tests/`.
- Frontend component and type checks reside in `apps/desktop/`.
- Future end-to-end integration tests (Tauri desktop communicating with AI service) will be added here.
